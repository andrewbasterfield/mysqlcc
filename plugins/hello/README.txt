COMPILE INSTRUCTIONS FOR WIN32 & Visual C++

From the command prompt type:

qmake -t vclib -o hello_plugin.dsp hello_plugin.pro

That will generate a file called hello_plugin.dsp which can be opened by Visual C++